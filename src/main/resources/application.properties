# -----------------------
# APPLICATION NAME
# -----------------------
spring.application.name=Payroll

# -----------------------
# DATABASE CONFIGURATION
# -----------------------
spring.datasource.url=jdbc:mysql://localhost:3306/payroll?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=Anil
spring.datasource.password=Anil@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# -----------------------
# JPA / HIBERNATE CONFIGURATION
# -----------------------
spring.jpa.hibernate.ddl-auto=update

# Show SQL queries in console
spring.jpa.show-sql=true

# Format SQL queries in console
spring.jpa.properties.hibernate.format_sql=true

# Correct Hibernate Dialect for MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
